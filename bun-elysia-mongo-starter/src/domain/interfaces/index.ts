// Domain ports (interfaces) implemented by infrastructure gateways/adapters.
// Define your service contracts here so use cases depend on abstractions, not
// on infrastructure. Example:
//   export interface IExampleService { doThing(id: number): Promise<void> }
export {}
