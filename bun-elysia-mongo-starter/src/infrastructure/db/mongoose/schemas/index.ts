// Barrel for all Mongoose schemas. Export every schema here so the container
// and repositories can import them from one place. Add yours as you build, e.g.:
//   export { exampleSchema, type IExample, type TExampleDocument } from './example'
export { commonFields } from './common-fields'
