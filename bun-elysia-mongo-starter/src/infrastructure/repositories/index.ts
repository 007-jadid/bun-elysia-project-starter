// Barrel for repository implementations (infrastructure adapters for the
// domain repository ports). Add your factories here as you build features, e.g.:
//   export { createExampleRepository } from './example.impl'
export {}
