// Barrel for all Drizzle table definitions. Import every table file here so the
// combined schema is available to both Drizzle instances and drizzle-kit.
//
// Add your tables as you build features, e.g.:
//   export * from "./example.table";
//
// This file MUST have at least one statement for `import * as schema` to work,
// so the empty object below stands in until you add your first table.
export {};
